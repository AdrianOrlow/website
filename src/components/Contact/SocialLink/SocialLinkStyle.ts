import styled from 'styled-components';
import { ButtonStyle } from '../../../shared/Button';

export const Container = styled(ButtonStyle)`
  padding: 0.75rem;
  display: flex;
  justify-content: center;
  align-items: center;
    
  svg {
    height: 1.25rem;
    width: 1.25rem;
  }
`;
